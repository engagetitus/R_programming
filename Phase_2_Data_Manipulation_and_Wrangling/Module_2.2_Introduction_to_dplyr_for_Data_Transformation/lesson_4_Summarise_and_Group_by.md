# Lesson 4: Summarise and Group by

* `summarise()` (or `summarize()`): Calculating aggregated statistics.
* `group_by()`: Performing operations on subsets of data.
* Combining `group_by()` and `summarise()`.
