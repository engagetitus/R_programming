# Lesson 3: Mutate and Arrange

* `mutate()`: Creating new variables or modifying existing ones.
* `transmute()`: Creating new variables while dropping others.
* `arrange()`: Reordering rows based on column values.
