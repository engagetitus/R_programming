# Lesson 2: Introduction to stringr

* Overview of the `stringr` package for consistent string manipulation.
* Common `stringr` functions (e.g., `str_detect()`, `str_replace()`, `str_subset()`, `str_extract()`).
