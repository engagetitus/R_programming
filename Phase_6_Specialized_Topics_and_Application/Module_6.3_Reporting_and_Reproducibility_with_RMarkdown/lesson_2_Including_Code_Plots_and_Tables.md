# Lesson 2: Including Code, Plots, and Tables

* Controlling code chunk options (`echo`, `eval`, `include`, `message`, `warning`).
* Embedding R plots directly.
* Creating formatted tables (e.g., using `knitr::kable`, `flextable`).
